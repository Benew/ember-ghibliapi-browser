{{#paper-button secondary=true iconButton=true onClick=(action "toggleFavorite")}}
  {{#if isFavorite}}
    {{paper-icon "favorite"}}
  {{else}}
    {{paper-icon "favorite outline"}}
  {{/if}}
{{/paper-button}}